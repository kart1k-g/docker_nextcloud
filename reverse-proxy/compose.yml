services:
  nginx:
    image: nginx:1.28

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./letsencrypt:/etc/letsencrypt:ro

    restart: unless-stopped

    network:
      - proxy
